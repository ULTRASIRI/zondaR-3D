export const MODEL_PATH = '/models/pagani_zonda_r_optimized.glb'

export const CAMERA = {
  position: [0, 1.5, 8],
  fov: 40,
  near: 0.1,
  far: 200,
}

export const TARGET_SCALE = 4.2

export const ORBIT_LIMITS = {
  minPolarAngle: Math.PI * 0.28,
  maxPolarAngle: Math.PI * 0.52,
  minAzimuthAngle: -Math.PI * 0.35,
  maxAzimuthAngle: Math.PI * 0.35,
}
